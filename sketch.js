var QUAD_WIDTH, QUAD_WIDTH_HALF, QUAD_HEIGHT, NUM_ROWS;

function setup() {
    createCanvas(1400, 600);
}

function draw() {

    for (var i = 0; i <= height; i++) {
      for (var j = 0; j <= width; j++) {
        fill(0, 13, 26);
      }
    }

}
